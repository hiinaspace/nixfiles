diff --git a/build.rs b/build.rs
index 5a1780dfe729b49484e888ba531bc577fa6e9d94..59ab7fae1418f2ebedc10f6eca97f06911064e1f 100644
--- a/build.rs
+++ b/build.rs
@@ -3,6 +3,12 @@ use std::env;
 use vergen_gitcl::{Emitter, GitclBuilder};
 
 fn main() -> Result<(), anyhow::Error> {
+    if env::var_os("CARGO_FEATURE_STATIC_OPENXR").is_some()
+        && env::var("CARGO_CFG_TARGET_OS").as_deref() == Ok("linux")
+    {
+        println!("cargo::rustc-link-arg=-lstdc++fs");
+    }
+
     let out_dir = env::var("OUT_DIR").unwrap();
     for path in shaders::compile(&out_dir) {
         println!("cargo::rerun-if-changed={}", path.to_str().unwrap());
diff --git a/src/compositor.rs b/src/compositor.rs
index 6eaebd687786025a12d23b06a204d71df8bc6630..65b10575b69ec45c282a1acf2189ba1b922d9f18 100644
--- a/src/compositor.rs
+++ b/src/compositor.rs
@@ -582,8 +582,12 @@ impl vr::IVRCompositor029_Interface for Compositor {
         crate::warn_unimplemented!("GetFrameTimeRemaining");
         0.0
     }
-    fn GetFrameTimings(&self, _pTiming: *mut vr::Compositor_FrameTiming, _nFrames: u32) -> u32 {
-        todo!()
+    fn GetFrameTimings(&self, timing: *mut vr::Compositor_FrameTiming, n_frames: u32) -> u32 {
+        if n_frames == 0 {
+            return 0;
+        }
+
+        u32::from(self.GetFrameTiming(timing, 0))
     }
     fn GetFrameTiming(&self, timing: *mut vr::Compositor_FrameTiming, _frames_ago: u32) -> bool {
         if timing.is_null() || !timing.is_aligned() {
@@ -1755,6 +1759,23 @@ mod tests {
         assert!(!f.comp.GetFrameTiming(timing.as_mut_ptr(), 1));
     }
 
+    #[test]
+    fn get_frame_timings() {
+        let f = Fixture::new();
+        assert_eq!(f.wait_get_poses(), None);
+        assert_eq!(f.submit(vr::EVREye::Left), None);
+        assert_eq!(f.submit(vr::EVREye::Right), None);
+
+        let mut timing = MaybeUninit::new(vr::Compositor_FrameTiming::default());
+        unsafe {
+            (&raw mut (*timing.as_mut_ptr()).m_nSize)
+                .write(std::mem::size_of::<vr::Compositor_FrameTiming>() as u32);
+        }
+        assert_eq!(f.comp.GetFrameTimings(timing.as_mut_ptr(), 1), 1);
+        assert_eq!(f.comp.GetFrameTimings(timing.as_mut_ptr(), 0), 0);
+        assert_eq!(f.comp.GetFrameTimings(std::ptr::null_mut(), 1), 0);
+    }
+
     #[test]
     fn zero_dims_texture() {
         let f = Fixture::new();
diff --git a/src/input.rs b/src/input.rs
index 77ab6ed2769825f4189ef22a12cb508eed54a50c..072a9a04bece81e05946d00b4fe16c21876f08f2 100644
--- a/src/input.rs
+++ b/src/input.rs
@@ -67,7 +67,12 @@ pub struct Input<C: openxr_data::Compositor> {
 struct InputEvent {
     ty: vr::EVREventType,
     index: vr::TrackedDeviceIndex_t,
-    data: vr::VREvent_Controller_t,
+    data: InputEventData,
+}
+
+enum InputEventData {
+    Controller(vr::VREvent_Controller_t),
+    Keyboard(vr::VREvent_Keyboard_t),
 }
 
 #[derive(Debug)]
@@ -1500,6 +1505,7 @@ impl<C: openxr_data::Compositor> Input<C> {
                     .unwrap();
 
                 self.legacy_state.on_action_sync();
+                self.legacy_state.on_direct_input_action_sync();
             }
             None => {
                 if self.loading_actions.load(Ordering::Relaxed) {
@@ -1572,20 +1578,27 @@ impl<C: openxr_data::Compositor> Input<C> {
                         vr::EVREventType::TrackedDeviceDeactivated
                     },
                     index: i as vr::TrackedDeviceIndex_t,
-                    data: Default::default(),
+                    data: InputEventData::Controller(Default::default()),
                 });
             }
         }
 
-        if let Some(event) = self.events.lock().unwrap().pop_front() {
-            const MIN_CONTROLLER_EVENT_SIZE: usize = std::mem::offset_of!(vr::VREvent_t, data)
-                + std::mem::size_of::<vr::VREvent_Controller_t>();
-            if size < MIN_CONTROLLER_EVENT_SIZE as u32 {
+        let mut events = self.events.lock().unwrap();
+        if let Some(event) = events.front() {
+            let required_size = std::mem::offset_of!(vr::VREvent_t, data)
+                + match &event.data {
+                    InputEventData::Controller(_) => {
+                        std::mem::size_of::<vr::VREvent_Controller_t>()
+                    }
+                    InputEventData::Keyboard(_) => std::mem::size_of::<vr::VREvent_Keyboard_t>(),
+                };
+            if size < required_size as u32 {
                 warn!(
-                    "{FUNC}: Provided event struct size ({size}) is smaller than required ({MIN_CONTROLLER_EVENT_SIZE})."
+                    "{FUNC}: Provided event struct size ({size}) is smaller than required ({required_size})."
                 );
                 return false;
             }
+            let event = events.pop_front().unwrap();
             // VREvent_t can be different sizes depending on the OpenVR version,
             // so we use raw pointers to avoid creating a reference, because if the
             // size doesn't match our VREvent_t's size, we are in UB land
@@ -1593,13 +1606,36 @@ impl<C: openxr_data::Compositor> Input<C> {
                 (&raw mut (*out).eventType).write(event.ty as u32);
                 (&raw mut (*out).trackedDeviceIndex).write(event.index);
                 (&raw mut (*out).eventAgeSeconds).write(0.0);
-                (&raw mut (*out).data.controller).write(event.data);
+                match event.data {
+                    InputEventData::Controller(data) => {
+                        (&raw mut (*out).data.controller).write(data);
+                    }
+                    InputEventData::Keyboard(data) => {
+                        (&raw mut (*out).data.keyboard).write(data);
+                    }
+                }
             }
             true
         } else {
             false
         }
     }
+
+    pub(crate) fn enqueue_keyboard_done(
+        &self,
+        user_value: u64,
+        overlay_handle: vr::VROverlayHandle_t,
+    ) {
+        self.events.lock().unwrap().push_back(InputEvent {
+            ty: vr::EVREventType::KeyboardDone,
+            index: vr::k_unTrackedDeviceIndexInvalid,
+            data: InputEventData::Keyboard(vr::VREvent_Keyboard_t {
+                cNewInput: [0; 8],
+                uUserValue: user_value,
+                overlayHandle: overlay_handle,
+            }),
+        });
+    }
 }
 
 enum LoadedActions {
diff --git a/src/input/devices.rs b/src/input/devices.rs
index ca232509a9df59b74b7b6c4875622816a6cd3f43..ffd14450944071a7615cb05d64117198c0997db3 100644
--- a/src/input/devices.rs
+++ b/src/input/devices.rs
@@ -8,6 +8,8 @@ use openvr as vr;
 use openxr as xr;
 
 use crate::input::profiles::knuckles::Knuckles;
+use crate::input::profiles::oculus_touch::OculusTouch;
+use crate::input::profiles::vive_controller::ViveWands;
 #[cfg(feature = "monado")]
 use crate::input::profiles::vive_tracker::ViveTracker;
 #[cfg(feature = "monado")]
@@ -20,6 +22,30 @@ use log::trace;
 
 use super::{Input, InteractionProfile, profiles::MainAxisType};
 
+/// Some legacy OpenVR games require a controller type for which they ship a
+/// control map. Keep Index OpenXR bindings while changing only that facade.
+#[derive(Clone, Copy)]
+enum KnucklesLegacyFacade {
+    Native,
+    OculusTouch,
+    ViveWands,
+}
+
+fn knuckles_legacy_facade() -> KnucklesLegacyFacade {
+    if std::env::var_os("XRIZER_OPENVR_KNUCKLES_AS_OCULUS_TOUCH").is_some_and(|value| value == "1")
+    {
+        KnucklesLegacyFacade::OculusTouch
+    } else if std::env::var_os("XRIZER_OPENVR_KNUCKLES_AS_VIVE").is_some_and(|value| value == "1") {
+        KnucklesLegacyFacade::ViveWands
+    } else {
+        KnucklesLegacyFacade::Native
+    }
+}
+
+pub(crate) fn knuckles_as_oculus_touch() -> bool {
+    matches!(knuckles_legacy_facade(), KnucklesLegacyFacade::OculusTouch)
+}
+
 pub enum TrackedDeviceType {
     Hmd,
     Controller {
@@ -61,8 +87,20 @@ pub struct ProfileData {
 
 impl ProfileData {
     pub fn new<P: InteractionProfile>() -> Self {
+        let properties = if TypeId::of::<P>() == TypeId::of::<Knuckles>() {
+            match knuckles_legacy_facade() {
+                KnucklesLegacyFacade::Native => P::properties(),
+                KnucklesLegacyFacade::OculusTouch => OculusTouch::properties(),
+                KnucklesLegacyFacade::ViveWands => ViveWands::properties(),
+            }
+        } else {
+            P::properties()
+        };
+
         Self {
-            properties: P::properties(),
+            // P remains Knuckles, so binding suggestions still target the
+            // active Valve Index OpenXR interaction profile.
+            properties,
             get_hand_offset: P::offset_grip_pose,
             force_estimated_thumb: TypeId::of::<P>() == TypeId::of::<Knuckles>(),
         }
diff --git a/src/input/legacy.rs b/src/input/legacy.rs
index 9610aa8a4853125a25ebd0ac8bbeba6b5f8f3eb1..b3872734228b4cf4a8a887a2712f15a49a318e93 100644
--- a/src/input/legacy.rs
+++ b/src/input/legacy.rs
@@ -4,17 +4,147 @@ use crate::{
         LoadedActions, ManifestLoadedActions,
         profiles::{self, RunWithProfile},
     },
-    openxr_data::{self},
+    openxr_data::{self, Hand},
 };
 use log::{debug, trace, warn};
 use openvr as vr;
 use openxr as xr;
+use std::fs::{self, OpenOptions};
+use std::io::Read;
+use std::os::unix::{
+    ffi::OsStrExt,
+    fs::{MetadataExt, OpenOptionsExt},
+};
+use std::path::{Path, PathBuf};
 use std::sync::atomic::{AtomicBool, AtomicU32, Ordering};
+use std::sync::{Mutex, OnceLock};
+
+const INPUT_COMMAND_ENV: &str = "STVR_XRIZER_INPUT_COMMAND";
+const MAX_INPUT_COMMAND_PATH_BYTES: usize = 4096;
+const MAX_INPUT_COMMAND_BYTES: usize = b"trigger".len();
+
+#[derive(Clone, Copy, Debug, Eq, PartialEq)]
+enum InputCommand {
+    Menu,
+    Trigger,
+}
+
+impl InputCommand {
+    fn button(self) -> vr::EVRButtonId {
+        match self {
+            // Skyrim VR maps RaceSex XButton to legacy OpenVR button 0x02.
+            // That is Grip for both Oculus hands in controlmapvr.txt; the
+            // simple-controller menu action reaches that same legacy facade.
+            Self::Menu => vr::EVRButtonId::Grip,
+            Self::Trigger => vr::EVRButtonId::SteamVR_Trigger,
+        }
+    }
+
+    fn affects_button(self, id: vr::EVRButtonId) -> bool {
+        self.button() == id
+    }
+}
+
+#[derive(Clone, Copy, Debug, Eq, PartialEq)]
+struct DirectInputUpdate {
+    active: Option<InputCommand>,
+    previous: Option<InputCommand>,
+}
+
+fn input_command_path() -> Option<PathBuf> {
+    valid_input_command_path(PathBuf::from(std::env::var_os(INPUT_COMMAND_ENV)?))
+}
+
+fn valid_input_command_path(path: PathBuf) -> Option<PathBuf> {
+    let bytes = path.as_os_str().as_bytes();
+    (path.is_absolute() && !bytes.is_empty() && bytes.len() <= MAX_INPUT_COMMAND_PATH_BYTES)
+        .then_some(path)
+}
+
+fn same_file(a: &fs::Metadata, b: &fs::Metadata) -> bool {
+    a.dev() == b.dev() && a.ino() == b.ino()
+}
+
+#[derive(Clone, Copy, Eq, PartialEq)]
+struct InputCommandFileIdentity {
+    dev: u64,
+    ino: u64,
+}
+
+impl From<&fs::Metadata> for InputCommandFileIdentity {
+    fn from(metadata: &fs::Metadata) -> Self {
+        Self {
+            dev: metadata.dev(),
+            ino: metadata.ino(),
+        }
+    }
+}
+
+/// Read and consume one producer-published direct input command. Bad, incomplete,
+/// or replaced files remain in place so their producer can diagnose the timeout.
+fn consume_input_command(
+    path: &Path,
+) -> Result<Option<InputCommand>, (Option<InputCommandFileIdentity>, &'static str)> {
+    let path_metadata = match fs::symlink_metadata(path) {
+        Ok(metadata) => metadata,
+        Err(error) if error.kind() == std::io::ErrorKind::NotFound => return Ok(None),
+        Err(_) => return Err((None, "could not inspect command path")),
+    };
+    let identity = Some(InputCommandFileIdentity::from(&path_metadata));
+    if !path_metadata.file_type().is_file() || path_metadata.len() > MAX_INPUT_COMMAND_BYTES as u64
+    {
+        return Err((identity, "path is not a bounded regular file"));
+    }
+
+    let mut file = OpenOptions::new()
+        .read(true)
+        .custom_flags(libc::O_NOFOLLOW | libc::O_CLOEXEC)
+        .open(path)
+        .map_err(|_| (identity, "could not safely open command file"))?;
+    let file_metadata = file
+        .metadata()
+        .map_err(|_| (identity, "could not inspect opened command file"))?;
+    if !file_metadata.file_type().is_file()
+        || file_metadata.len() > MAX_INPUT_COMMAND_BYTES as u64
+        || !same_file(&path_metadata, &file_metadata)
+    {
+        return Err((identity, "command file changed while opening"));
+    }
+
+    let mut bytes = [0; MAX_INPUT_COMMAND_BYTES + 1];
+    let count = file
+        .read(&mut bytes)
+        .map_err(|_| (identity, "could not read command file"))?;
+    let command = match &bytes[..count] {
+        b"menu" => InputCommand::Menu,
+        b"trigger" => InputCommand::Trigger,
+        _ => return Err((identity, "command must be exactly `menu` or `trigger`")),
+    };
+
+    // Do not remove an atomically replaced file. The normal publisher never
+    // replaces a pending command, but this check keeps the opt-in path safe.
+    let current_metadata = fs::symlink_metadata(path)
+        .map_err(|_| (identity, "command file disappeared while consuming"))?;
+    if !current_metadata.file_type().is_file() || !same_file(&file_metadata, &current_metadata) {
+        return Err((identity, "command file changed while consuming"));
+    }
+    fs::remove_file(path).map_err(|_| (identity, "could not consume command file"))?;
+    Ok(Some(command))
+}
+
+#[derive(Default)]
+struct DirectInputState {
+    active: Option<InputCommand>,
+    observed: [Option<InputCommand>; 2],
+    rejected_command: Option<InputCommandFileIdentity>,
+}
 
 #[derive(Default)]
 pub(super) struct LegacyState {
     packet_num: AtomicU32,
     got_state_this_frame: [AtomicBool; 2],
+    input_command_path: OnceLock<Option<PathBuf>>,
+    direct_input: Mutex<DirectInputState>,
 }
 
 impl LegacyState {
@@ -24,6 +154,92 @@ impl LegacyState {
             state.store(false, Ordering::Relaxed);
         }
     }
+
+    fn take_input_command(&self, direct_input: &mut DirectInputState) -> Option<InputCommand> {
+        let path = self.input_command_path.get_or_init(input_command_path);
+        let Some(path) = path.as_deref() else {
+            return None;
+        };
+        match consume_input_command(path) {
+            Ok(Some(command)) => {
+                direct_input.rejected_command = None;
+                Some(command)
+            }
+            Ok(None) => {
+                direct_input.rejected_command = None;
+                None
+            }
+            Err((identity, reason)) => {
+                if direct_input.rejected_command != identity {
+                    warn!(
+                        "ignoring malformed direct input command at {}: {reason}",
+                        path.display()
+                    );
+                    direct_input.rejected_command = identity;
+                }
+                None
+            }
+        }
+    }
+
+    /// Consume commands only at a completed legacy action sync. A command stays
+    /// pressed for every controller-state poll until the next sync. Per-hand
+    /// observations let event composition suppress a release for a hand that
+    /// never observed the synthetic press.
+    pub fn on_direct_input_action_sync(&self) {
+        let mut direct_input = self.direct_input.lock().unwrap();
+        if let Some(command) = direct_input.active.take() {
+            debug!("releasing direct input command {command:?}");
+            return;
+        }
+
+        if let Some(command) = self.take_input_command(&mut direct_input) {
+            debug!("arming and pressing direct input command {command:?}");
+            direct_input.active = Some(command);
+        }
+    }
+
+    fn direct_input_for_hand(&self, hand: Hand, record_transition: bool) -> DirectInputUpdate {
+        let mut direct_input = self.direct_input.lock().unwrap();
+        let active = direct_input.active;
+        let previous = if record_transition {
+            let observed = &mut direct_input.observed[hand as usize - 1];
+            let previous = *observed;
+            *observed = active;
+            previous
+        } else {
+            active
+        };
+        DirectInputUpdate { active, previous }
+    }
+}
+
+#[derive(Clone, Copy, Debug, Eq, PartialEq)]
+struct CombinedButtonState {
+    pressed: bool,
+    changed: bool,
+}
+
+fn combine_button_state(
+    real_pressed: bool,
+    real_changed: bool,
+    command: DirectInputUpdate,
+    id: vr::EVRButtonId,
+) -> CombinedButtonState {
+    let command_pressed = command.active.is_some_and(|value| value.affects_button(id));
+    let previous_command_pressed = command
+        .previous
+        .is_some_and(|value| value.affects_button(id));
+    let previous_real_pressed = if real_changed {
+        !real_pressed
+    } else {
+        real_pressed
+    };
+    let pressed = real_pressed || command_pressed;
+    CombinedButtonState {
+        pressed,
+        changed: pressed != (previous_real_pressed || previous_command_pressed),
+    }
 }
 
 // Adapted from openvr.h
@@ -214,10 +430,13 @@ impl<C: openxr_data::Compositor> Input<C> {
         state.unPacketNum = self.legacy_state.packet_num.load(Ordering::Relaxed);
 
         // Only send the input event if we haven't already.
-        let mut events = self.legacy_state.got_state_this_frame[hand as usize - 1]
+        let record_transition = self.legacy_state.got_state_this_frame[hand as usize - 1]
             .compare_exchange(false, true, Ordering::Relaxed, Ordering::Relaxed)
-            .is_ok()
-            .then(|| self.events.lock().unwrap());
+            .is_ok();
+        let mut events = record_transition.then(|| self.events.lock().unwrap());
+        let command = self
+            .legacy_state
+            .direct_input_for_hand(hand, record_transition);
 
         let mut read_button =
             |id, click_action: &xr::Action<bool>, touch_action: Option<&xr::Action<bool>>| {
@@ -226,7 +445,13 @@ impl<C: openxr_data::Compositor> Input<C> {
                 state.ulButtonTouched |= button_mask_from_id(id) & (touched as u64 * u64::MAX);
 
                 let click_state = click_action.state(&data.session, hand_path).unwrap();
-                let pressed = click_state.current_state;
+                let combined = combine_button_state(
+                    click_state.current_state,
+                    click_state.changed_since_last_sync,
+                    command,
+                    id,
+                );
+                let pressed = combined.pressed;
                 state.ulButtonPressed |= button_mask_from_id(id) & (pressed as u64 * u64::MAX);
 
                 if let Some(events) = &mut events {
@@ -238,10 +463,12 @@ impl<C: openxr_data::Compositor> Input<C> {
                                 vr::EVREventType::ButtonUntouch
                             },
                             index: device_index,
-                            data: vr::VREvent_Controller_t { button: id as u32 },
+                            data: super::InputEventData::Controller(vr::VREvent_Controller_t {
+                                button: id as u32,
+                            }),
                         });
                     }
-                    if click_state.changed_since_last_sync {
+                    if combined.changed {
                         events.push_back(super::InputEvent {
                             ty: if pressed {
                                 vr::EVREventType::ButtonPress
@@ -249,7 +476,9 @@ impl<C: openxr_data::Compositor> Input<C> {
                                 vr::EVREventType::ButtonUnpress
                             },
                             index: device_index,
-                            data: vr::VREvent_Controller_t { button: id as u32 },
+                            data: super::InputEventData::Controller(vr::VREvent_Controller_t {
+                                button: id as u32,
+                            }),
                         });
                     }
                 }
@@ -278,7 +507,11 @@ impl<C: openxr_data::Compositor> Input<C> {
 
         let t = actions.trigger.state(&data.session, hand_path).unwrap();
         state.rAxis[1] = vr::VRControllerAxis_t {
-            x: t.current_state,
+            x: if command.active == Some(InputCommand::Trigger) {
+                t.current_state.max(1.0)
+            } else {
+                t.current_state
+            },
             y: 0.0,
         };
 
@@ -420,6 +653,7 @@ impl LegacyActionData {
 
 #[cfg(test)]
 mod tests {
+    use super::*;
     use crate::input::profiles::{knuckles::Knuckles, simple_controller::SimpleController};
     use crate::input::tests::{Fixture, compare_pose};
     use crate::openxr_data::Hand;
@@ -463,6 +697,250 @@ mod tests {
         verify_offset!(data, data);
     };
 
+    fn test_command_path(name: &str) -> std::path::PathBuf {
+        let nonce = std::time::SystemTime::now()
+            .duration_since(std::time::UNIX_EPOCH)
+            .unwrap()
+            .as_nanos();
+        std::env::temp_dir().join(format!(
+            "xrizer-input-command-{name}-{}-{nonce}",
+            std::process::id()
+        ))
+    }
+
+    #[test]
+    fn direct_input_command_requires_an_absolute_bounded_path() {
+        assert!(valid_input_command_path(PathBuf::from("relative-command")).is_none());
+        assert!(valid_input_command_path(PathBuf::from("/")).is_some());
+        assert!(
+            valid_input_command_path(PathBuf::from(format!(
+                "/{}",
+                "x".repeat(MAX_INPUT_COMMAND_PATH_BYTES)
+            )))
+            .is_none()
+        );
+    }
+
+    #[test]
+    fn direct_input_command_is_parsed_then_consumed_once() {
+        let path = test_command_path("parse");
+        let state = LegacyState::default();
+        state.input_command_path.set(Some(path.clone())).unwrap();
+
+        fs::write(&path, b"menu").unwrap();
+        assert_eq!(
+            state.take_input_command(&mut state.direct_input.lock().unwrap()),
+            Some(InputCommand::Menu)
+        );
+        assert!(!path.exists());
+        assert_eq!(
+            state.take_input_command(&mut state.direct_input.lock().unwrap()),
+            None
+        );
+
+        fs::write(&path, b"trigger").unwrap();
+        assert_eq!(
+            state.take_input_command(&mut state.direct_input.lock().unwrap()),
+            Some(InputCommand::Trigger)
+        );
+        assert!(!path.exists());
+
+        fs::write(&path, b"menu\n").unwrap();
+        assert_eq!(
+            state.take_input_command(&mut state.direct_input.lock().unwrap()),
+            None
+        );
+        assert!(path.exists());
+        fs::remove_file(path).unwrap();
+    }
+
+    #[test]
+    fn direct_input_command_rejects_a_symlink() {
+        let target = test_command_path("target");
+        let link = test_command_path("link");
+        fs::write(&target, b"menu").unwrap();
+        std::os::unix::fs::symlink(&target, &link).unwrap();
+
+        let state = LegacyState::default();
+        state.input_command_path.set(Some(link.clone())).unwrap();
+        assert_eq!(
+            state.take_input_command(&mut state.direct_input.lock().unwrap()),
+            None
+        );
+        assert!(link.exists());
+        assert!(target.exists());
+
+        fs::remove_file(link).unwrap();
+        fs::remove_file(target).unwrap();
+    }
+
+    #[test]
+    fn direct_input_command_uses_skyrim_racesex_grip_button() {
+        assert_eq!(InputCommand::Menu.button(), vr::EVRButtonId::Grip);
+        assert_eq!(InputCommand::Menu.button() as u32, 0x02);
+    }
+
+    #[test]
+    fn direct_input_command_stays_pressed_for_repeated_polls_from_both_hands() {
+        let path = test_command_path("both-hands");
+        let state = LegacyState::default();
+        state.input_command_path.set(Some(path.clone())).unwrap();
+        fs::write(&path, b"trigger").unwrap();
+        state.on_direct_input_action_sync();
+        assert!(!path.exists());
+
+        // Either polling order sees the same level for the complete sync frame.
+        assert_eq!(
+            state.direct_input_for_hand(Hand::Right, true),
+            DirectInputUpdate {
+                active: Some(InputCommand::Trigger),
+                previous: None,
+            }
+        );
+        assert_eq!(
+            state.direct_input_for_hand(Hand::Left, true),
+            DirectInputUpdate {
+                active: Some(InputCommand::Trigger),
+                previous: None,
+            }
+        );
+        assert_eq!(
+            state.direct_input_for_hand(Hand::Right, false),
+            DirectInputUpdate {
+                active: Some(InputCommand::Trigger),
+                previous: Some(InputCommand::Trigger),
+            }
+        );
+        assert_eq!(
+            state.direct_input_for_hand(Hand::Left, false),
+            DirectInputUpdate {
+                active: Some(InputCommand::Trigger),
+                previous: Some(InputCommand::Trigger),
+            }
+        );
+    }
+
+    #[test]
+    fn direct_input_command_has_observed_press_and_release_transitions() {
+        let path = test_command_path("press-duration");
+        let state = LegacyState::default();
+        state.input_command_path.set(Some(path.clone())).unwrap();
+        fs::write(&path, b"trigger").unwrap();
+        state.on_direct_input_action_sync();
+        let press = state.direct_input_for_hand(Hand::Left, true);
+        assert_eq!(
+            combine_button_state(false, false, press, vr::EVRButtonId::SteamVR_Trigger),
+            CombinedButtonState {
+                pressed: true,
+                changed: true,
+            }
+        );
+
+        state.on_direct_input_action_sync();
+        let release = state.direct_input_for_hand(Hand::Left, true);
+        assert_eq!(
+            combine_button_state(false, false, release, vr::EVRButtonId::SteamVR_Trigger),
+            CombinedButtonState {
+                pressed: false,
+                changed: true,
+            }
+        );
+    }
+
+    #[test]
+    fn direct_input_composes_with_held_and_simultaneous_physical_input() {
+        let synthetic_press = DirectInputUpdate {
+            active: Some(InputCommand::Trigger),
+            previous: None,
+        };
+        let synthetic_release = DirectInputUpdate {
+            active: None,
+            previous: Some(InputCommand::Trigger),
+        };
+
+        // A pre-existing physical hold must not receive a duplicate press or a
+        // synthetic release while that physical control remains held.
+        assert_eq!(
+            combine_button_state(
+                true,
+                false,
+                synthetic_press,
+                vr::EVRButtonId::SteamVR_Trigger,
+            ),
+            CombinedButtonState {
+                pressed: true,
+                changed: false,
+            }
+        );
+        assert_eq!(
+            combine_button_state(
+                true,
+                false,
+                synthetic_release,
+                vr::EVRButtonId::SteamVR_Trigger,
+            ),
+            CombinedButtonState {
+                pressed: true,
+                changed: false,
+            }
+        );
+
+        // A real and synthetic press in the same sync produces one combined edge.
+        assert_eq!(
+            combine_button_state(
+                true,
+                true,
+                synthetic_press,
+                vr::EVRButtonId::SteamVR_Trigger,
+            ),
+            CombinedButtonState {
+                pressed: true,
+                changed: true,
+            }
+        );
+    }
+
+    #[test]
+    fn unobserved_direct_input_press_does_not_emit_a_release() {
+        let path = test_command_path("unobserved");
+        let state = LegacyState::default();
+        state.input_command_path.set(Some(path.clone())).unwrap();
+        fs::write(&path, b"menu").unwrap();
+        state.on_direct_input_action_sync();
+        state.on_direct_input_action_sync();
+
+        assert_eq!(
+            state.direct_input_for_hand(Hand::Left, true),
+            DirectInputUpdate {
+                active: None,
+                previous: None,
+            }
+        );
+    }
+
+    #[test]
+    fn direct_input_command_does_not_change_state_without_a_command_file() {
+        let path = test_command_path("no-command");
+        let state = LegacyState::default();
+        state.input_command_path.set(Some(path)).unwrap();
+
+        state.on_direct_input_action_sync();
+        assert_eq!(
+            state.direct_input_for_hand(Hand::Left, true),
+            DirectInputUpdate {
+                active: None,
+                previous: None,
+            }
+        );
+        assert_eq!(
+            state.direct_input_for_hand(Hand::Right, true),
+            DirectInputUpdate {
+                active: None,
+                previous: None,
+            }
+        );
+    }
+
     #[test]
     fn no_legacy_input_before_session_setup() {
         let fixture = Fixture::new();
diff --git a/src/input/profiles/knuckles.rs b/src/input/profiles/knuckles.rs
index 4b297d5d4e1a280d96b06651301fe4827923480d..269cc23c600ceedae9055f5a108a48f78abcf08c 100644
--- a/src/input/profiles/knuckles.rs
+++ b/src/input/profiles/knuckles.rs
@@ -3,6 +3,7 @@ use super::{
     legal_paths, paths::*,
 };
 use crate::button_mask_from_ids;
+use crate::input::devices::knuckles_as_oculus_touch;
 use crate::input::legacy::{self, LegacyBindings, button_mask_from_id};
 use crate::input::profiles::DynInputPath;
 use crate::openxr_data::Hand;
@@ -97,7 +98,14 @@ impl InteractionProfile for Knuckles {
             app_menu: c.leftright::<B, Click, _, _>(),
             a: c.leftright::<A, Click, _, _>(),
             trigger: c.leftright::<Trigger, Value, _, _>(),
-            trigger_click: c.leftright::<Trigger, Click, _, _>(),
+            // The Oculus Touch facade is used by legacy games such as Skyrim
+            // VR that consume Axis1 as a pressed trigger. Monado's Index
+            // profile reliably supplies trigger value, but not its click bit.
+            trigger_click: if knuckles_as_oculus_touch() {
+                c.leftright::<Trigger, Value, _, _>()
+            } else {
+                c.leftright::<Trigger, Click, _, _>()
+            },
             squeeze: c.leftright::<Squeeze, Value, _, _>(),
             squeeze_click: c.leftright::<Squeeze, Value, _, _>(),
             main_xy: c.leftright::<Thumbstick, (), _, _>(),
diff --git a/src/input/tests.rs b/src/input/tests.rs
index c2160e78b74237df339cad10f725550f054adde3..d9ae598932b0dbe38d972d388bf542990325cf68 100644
--- a/src/input/tests.rs
+++ b/src/input/tests.rs
@@ -1029,6 +1029,44 @@ fn empty_manifest() {
     assert!(f.input.action_map.read().unwrap().is_empty());
 }
 
+#[test]
+fn keyboard_done_event_survives_an_undersized_poll() {
+    let f = Fixture::new();
+    f.input.events.lock().unwrap().clear();
+    f.input.enqueue_keyboard_done(0x1234, 0x5678);
+
+    let required_size =
+        std::mem::offset_of!(vr::VREvent_t, data) + std::mem::size_of::<vr::VREvent_Keyboard_t>();
+    let mut event = vr::VREvent_t::default();
+    assert!(
+        !f.input
+            .get_next_event((required_size - 1) as u32, &mut event)
+    );
+    assert!(matches!(
+        f.input
+            .events
+            .lock()
+            .unwrap()
+            .front()
+            .map(|event| &event.data),
+        Some(super::InputEventData::Keyboard(_))
+    ));
+
+    assert!(
+        f.input
+            .get_next_event(std::mem::size_of::<vr::VREvent_t>() as u32, &mut event)
+    );
+    let event_type = unsafe { (&raw const event.eventType).read_unaligned() };
+    let device_index = unsafe { (&raw const event.trackedDeviceIndex).read_unaligned() };
+    let keyboard = unsafe { (&raw const event.data.keyboard).read_unaligned() };
+    assert_eq!(event_type, vr::EVREventType::KeyboardDone as u32);
+    assert_eq!(device_index, vr::k_unTrackedDeviceIndexInvalid);
+    assert_eq!(keyboard.cNewInput, [0; 8]);
+    assert_eq!(keyboard.uUserValue, 0x1234);
+    assert_eq!(keyboard.overlayHandle, 0x5678);
+    f.input.events.lock().unwrap().clear();
+}
+
 #[test]
 fn load_actions_race() {
     let mut f = Fixture::new();
diff --git a/src/overlay.rs b/src/overlay.rs
index 528d8fd0632fab6080fd416e77405a99551fc2d8..9ccd11cf6f01ad24213c495b91f62092c39395b3 100644
--- a/src/overlay.rs
+++ b/src/overlay.rs
@@ -2,6 +2,7 @@ use crate::{
     clientcore::{Injected, Injector},
     compositor::{Compositor, is_usable_swapchain},
     graphics_backends::{GraphicsBackend, SupportedBackend, supported_apis_enum},
+    input::Input,
     openxr_data::{GraphicalSession, OpenXrData, Session, SessionData},
 };
 use glam::{Quat, Vec3, vec3};
@@ -12,7 +13,10 @@ use slotmap::{Key, KeyData, SecondaryMap, SlotMap, new_key_type};
 use std::f32::consts::{FRAC_1_SQRT_2, PI};
 use std::ffi::{CStr, CString, c_char, c_void};
 use std::sync::{Arc, Mutex, RwLock};
-use std::{collections::HashMap, ops::Deref};
+use std::{
+    collections::{HashMap, VecDeque},
+    ops::Deref,
+};
 
 // OpenVR overlays are allowed to use ≥ 0
 pub const SKYBOX_Z_ORDER: i64 = -1;
@@ -30,6 +34,7 @@ pub struct OverlayMan {
     overlays: RwLock<SlotMap<OverlayKey, Overlay>>,
     key_to_overlay: RwLock<HashMap<CString, OverlayKey>>,
     skybox: RwLock<Vec<OverlayKey>>,
+    keyboard_text: Mutex<Option<CString>>,
 }
 
 #[derive(derive_more::Deref)]
@@ -44,6 +49,7 @@ impl OverlayMan {
             overlays: Default::default(),
             key_to_overlay: Default::default(),
             skybox: Default::default(),
+            keyboard_text: Default::default(),
         }
     }
 
@@ -478,6 +484,110 @@ struct Overlay {
     transform: Option<(vr::ETrackingUniverseOrigin, vr::HmdMatrix34_t)>,
     compositor: Option<SupportedBackend>,
     rect: Option<xr::Rect2Di>,
+    events: VecDeque<OverlayEvent>,
+}
+
+#[derive(Clone, Copy)]
+struct OverlayEvent {
+    ty: vr::EVREventType,
+    data: vr::VREvent_Keyboard_t,
+}
+
+impl OverlayEvent {
+    fn keyboard_done(user_value: u64, overlay_handle: vr::VROverlayHandle_t) -> Self {
+        Self {
+            ty: vr::EVREventType::KeyboardDone,
+            data: vr::VREvent_Keyboard_t {
+                cNewInput: [0; 8],
+                uUserValue: user_value,
+                overlayHandle: overlay_handle,
+            },
+        }
+    }
+}
+
+fn poll_overlay_event(
+    events: &mut VecDeque<OverlayEvent>,
+    out: *mut vr::VREvent_t,
+    size: u32,
+) -> bool {
+    if out.is_null() {
+        return false;
+    }
+    let required_size =
+        std::mem::offset_of!(vr::VREvent_t, data) + std::mem::size_of::<vr::VREvent_Keyboard_t>();
+    if size < required_size as u32 || events.is_empty() {
+        return false;
+    }
+
+    let event = events.pop_front().unwrap();
+    unsafe {
+        (&raw mut (*out).eventType).write(event.ty as u32);
+        (&raw mut (*out).trackedDeviceIndex).write(vr::k_unTrackedDeviceIndexInvalid);
+        (&raw mut (*out).eventAgeSeconds).write(0.0);
+        (&raw mut (*out).data.keyboard).write(event.data);
+    }
+    true
+}
+
+fn copy_keyboard_text(text: Option<&CStr>, text_out: *mut c_char, text_capacity: u32) -> u32 {
+    let Some(text) = text else {
+        if !text_out.is_null() && text_capacity > 0 {
+            unsafe { text_out.write(0) };
+        }
+        return 0;
+    };
+
+    let bytes = text.to_bytes();
+    let required_size = u32::try_from(bytes.len() + 1).unwrap_or(u32::MAX);
+    if text_out.is_null() || text_capacity == 0 {
+        return required_size;
+    }
+
+    let utf8 = text
+        .to_str()
+        .expect("automation keyboard text was validated as UTF-8");
+    let mut copy_len = bytes.len().min(text_capacity.saturating_sub(1) as usize);
+    while !utf8.is_char_boundary(copy_len) {
+        copy_len -= 1;
+    }
+
+    unsafe {
+        std::ptr::copy_nonoverlapping(bytes.as_ptr().cast::<c_char>(), text_out, copy_len);
+        text_out.add(copy_len).write(0);
+    }
+    required_size
+}
+
+fn validate_keyboard_text(text: String, char_max: u32) -> Result<CString, vr::EVROverlayError> {
+    if text.chars().count() > char_max as usize {
+        log::warn!("STVR_XRIZER_KEYBOARD_TEXT exceeds the requested character limit");
+        return Err(vr::EVROverlayError::RequestFailed);
+    }
+
+    let text = CString::new(text).map_err(|_| {
+        log::warn!("STVR_XRIZER_KEYBOARD_TEXT contains an interior NUL");
+        vr::EVROverlayError::RequestFailed
+    })?;
+    if u32::try_from(text.as_bytes_with_nul().len()).is_err() {
+        return Err(vr::EVROverlayError::RequestFailed);
+    }
+
+    Ok(text)
+}
+
+fn keyboard_text_from_env(char_max: u32) -> Result<CString, vr::EVROverlayError> {
+    match std::env::var("STVR_XRIZER_KEYBOARD_TEXT") {
+        Ok(text) => validate_keyboard_text(text, char_max),
+        Err(std::env::VarError::NotPresent) => {
+            debug!("STVR_XRIZER_KEYBOARD_TEXT is not set; rejecting virtual keyboard input");
+            Err(vr::EVROverlayError::RequestFailed)
+        }
+        Err(std::env::VarError::NotUnicode(_)) => {
+            log::warn!("STVR_XRIZER_KEYBOARD_TEXT is not valid UTF-8");
+            Err(vr::EVROverlayError::RequestFailed)
+        }
+    }
 }
 
 impl Overlay {
@@ -499,6 +609,7 @@ impl Overlay {
             transform: None,
             compositor: None,
             rect: None,
+            events: Default::default(),
         }
     }
 
@@ -801,22 +912,41 @@ impl vr::IVROverlay028_Interface for OverlayMan {
     fn HideKeyboard(&self) {
         crate::warn_unimplemented!("HideKeyboard");
     }
-    fn GetKeyboardText(&self, _: *mut c_char, _: u32) -> u32 {
-        todo!()
+    fn GetKeyboardText(&self, text_out: *mut c_char, text_capacity: u32) -> u32 {
+        let keyboard_text = self.keyboard_text.lock().unwrap();
+        copy_keyboard_text(keyboard_text.as_deref(), text_out, text_capacity)
     }
     fn ShowKeyboardForOverlay(
         &self,
-        _: vr::VROverlayHandle_t,
+        overlay_handle: vr::VROverlayHandle_t,
         _: vr::EGamepadTextInputMode,
         _: vr::EGamepadTextInputLineMode,
         _: u32,
         _: *const c_char,
-        _: u32,
+        char_max: u32,
         _: *const c_char,
-        _: u64,
+        user_value: u64,
     ) -> vr::EVROverlayError {
-        crate::warn_unimplemented!("ShowKeyboardForOverlay");
-        vr::EVROverlayError::RequestFailed
+        let text = match keyboard_text_from_env(char_max) {
+            Ok(text) => text,
+            Err(error) => return error,
+        };
+
+        let overlay_key = OverlayKey::from(KeyData::from_ffi(overlay_handle));
+        let overlay_event = OverlayEvent::keyboard_done(user_value, overlay_handle);
+        let mut overlays = self.overlays.write().unwrap();
+        let Some(overlay) = overlays.get_mut(overlay_key) else {
+            return vr::EVROverlayError::UnknownOverlay;
+        };
+
+        *self.keyboard_text.lock().unwrap() = Some(text);
+        overlay.events.push_back(overlay_event);
+        drop(overlays);
+        self.openxr
+            .input
+            .force(|_| Input::new(self.openxr.clone()))
+            .enqueue_keyboard_done(user_value, overlay_handle);
+        vr::EVROverlayError::None
     }
     fn ShowKeyboard(
         &self,
@@ -824,12 +954,20 @@ impl vr::IVROverlay028_Interface for OverlayMan {
         _: vr::EGamepadTextInputLineMode,
         _: u32,
         _: *const c_char,
-        _: u32,
+        char_max: u32,
         _: *const c_char,
-        _: u64,
+        user_value: u64,
     ) -> vr::EVROverlayError {
-        crate::warn_unimplemented!("ShowKeyboard");
-        vr::EVROverlayError::RequestFailed
+        let text = match keyboard_text_from_env(char_max) {
+            Ok(text) => text,
+            Err(error) => return error,
+        };
+        *self.keyboard_text.lock().unwrap() = Some(text);
+        self.openxr
+            .input
+            .force(|_| Input::new(self.openxr.clone()))
+            .enqueue_keyboard_done(user_value, vr::k_ulOverlayHandleInvalid);
+        vr::EVROverlayError::None
     }
     fn GetPrimaryDashboardDevice(&self) -> vr::TrackedDeviceIndex_t {
         todo!()
@@ -1001,11 +1139,16 @@ impl vr::IVROverlay028_Interface for OverlayMan {
     }
     fn PollNextOverlayEvent(
         &self,
-        _: vr::VROverlayHandle_t,
-        _: *mut vr::VREvent_t,
-        _: u32,
+        overlay_handle: vr::VROverlayHandle_t,
+        event: *mut vr::VREvent_t,
+        event_size: u32,
     ) -> bool {
-        false
+        let overlay_key = OverlayKey::from(KeyData::from_ffi(overlay_handle));
+        let mut overlays = self.overlays.write().unwrap();
+        let Some(overlay) = overlays.get_mut(overlay_key) else {
+            return false;
+        };
+        poll_overlay_event(&mut overlay.events, event, event_size)
     }
     fn WaitFrameSync(&self, _: u32) -> vr::EVROverlayError {
         todo!()
@@ -1593,3 +1736,184 @@ impl vr::IVROverlay007On010 for OverlayMan {
         todo!()
     }
 }
+
+#[cfg(test)]
+mod tests {
+    use super::*;
+    use crate::{clientcore::Injector, input::Input, openxr_data::OpenXrData};
+    use std::{
+        ffi::OsString,
+        sync::{Arc, Mutex},
+    };
+
+    static KEYBOARD_ENV_LOCK: Mutex<()> = Mutex::new(());
+
+    struct KeyboardEnvReset(Option<OsString>);
+
+    impl Drop for KeyboardEnvReset {
+        fn drop(&mut self) {
+            unsafe {
+                if let Some(value) = self.0.take() {
+                    std::env::set_var("STVR_XRIZER_KEYBOARD_TEXT", value);
+                } else {
+                    std::env::remove_var("STVR_XRIZER_KEYBOARD_TEXT");
+                }
+            }
+        }
+    }
+
+    fn set_keyboard_env(value: Option<&str>) -> KeyboardEnvReset {
+        let original = std::env::var_os("STVR_XRIZER_KEYBOARD_TEXT");
+        unsafe {
+            if let Some(value) = value {
+                std::env::set_var("STVR_XRIZER_KEYBOARD_TEXT", value);
+            } else {
+                std::env::remove_var("STVR_XRIZER_KEYBOARD_TEXT");
+            }
+        }
+        KeyboardEnvReset(original)
+    }
+
+    fn global_keyboard_fixture() -> (OverlayMan, Arc<Input<Compositor>>) {
+        let injector = Injector::default();
+        let openxr = Arc::new(OpenXrData::new(&injector).unwrap());
+        let input = Arc::new(Input::new(openxr.clone()));
+        openxr.input.set(Arc::downgrade(&input));
+
+        (OverlayMan::new(openxr, &injector), input)
+    }
+
+    #[test]
+    fn missing_keyboard_text_returns_an_empty_result_without_panicking() {
+        let mut output = [1_i8; 4];
+        assert_eq!(copy_keyboard_text(None, output.as_mut_ptr(), 4), 0);
+        assert_eq!(output[0], 0);
+        assert_eq!(copy_keyboard_text(None, std::ptr::null_mut(), 0), 0);
+    }
+
+    #[test]
+    fn keyboard_text_reports_required_size_and_preserves_utf8_boundaries() {
+        let text = CString::new("Dovahkiin").unwrap();
+        assert_eq!(copy_keyboard_text(Some(&text), std::ptr::null_mut(), 0), 10);
+
+        let mut output = [0_i8; 5];
+        assert_eq!(copy_keyboard_text(Some(&text), output.as_mut_ptr(), 5), 10);
+        assert_eq!(
+            unsafe { CStr::from_ptr(output.as_ptr()) }.to_bytes(),
+            b"Dova"
+        );
+    }
+
+    #[test]
+    fn global_keyboard_publishes_text_and_system_keyboard_done_event() {
+        let _env_lock = KEYBOARD_ENV_LOCK.lock().unwrap();
+        let _env_reset = set_keyboard_env(Some("Dovahkiin"));
+        let (overlays, input) = global_keyboard_fixture();
+
+        assert_eq!(
+            <OverlayMan as vr::IVROverlay028_Interface>::ShowKeyboard(
+                &overlays,
+                vr::EGamepadTextInputMode::Normal,
+                vr::EGamepadTextInputLineMode::SingleLine,
+                0,
+                std::ptr::null(),
+                9,
+                std::ptr::null(),
+                0x1234,
+            ),
+            vr::EVROverlayError::None
+        );
+
+        let mut text = [0_i8; 16];
+        assert_eq!(
+            <OverlayMan as vr::IVROverlay028_Interface>::GetKeyboardText(
+                &overlays,
+                text.as_mut_ptr(),
+                text.len() as u32,
+            ),
+            10
+        );
+        assert_eq!(unsafe { CStr::from_ptr(text.as_ptr()) }, c"Dovahkiin");
+
+        let mut event = vr::VREvent_t::default();
+        assert!(input.get_next_event(std::mem::size_of::<vr::VREvent_t>() as u32, &mut event,));
+        let event_type = unsafe { (&raw const event.eventType).read_unaligned() };
+        let keyboard = unsafe { (&raw const event.data.keyboard).read_unaligned() };
+        assert_eq!(event_type, vr::EVREventType::KeyboardDone as u32);
+        assert_eq!(keyboard.uUserValue, 0x1234);
+        assert_eq!(keyboard.overlayHandle, vr::k_ulOverlayHandleInvalid);
+    }
+
+    #[test]
+    fn global_keyboard_without_automation_text_fails_closed() {
+        let _env_lock = KEYBOARD_ENV_LOCK.lock().unwrap();
+        let _env_reset = set_keyboard_env(None);
+        let (overlays, _input) = global_keyboard_fixture();
+
+        assert_eq!(
+            <OverlayMan as vr::IVROverlay028_Interface>::ShowKeyboard(
+                &overlays,
+                vr::EGamepadTextInputMode::Normal,
+                vr::EGamepadTextInputLineMode::SingleLine,
+                0,
+                std::ptr::null(),
+                16,
+                std::ptr::null(),
+                0,
+            ),
+            vr::EVROverlayError::RequestFailed
+        );
+        let mut text = [1_i8; 1];
+        assert_eq!(
+            <OverlayMan as vr::IVROverlay028_Interface>::GetKeyboardText(
+                &overlays,
+                text.as_mut_ptr(),
+                text.len() as u32,
+            ),
+            0
+        );
+        assert_eq!(text, [0]);
+    }
+
+    #[test]
+    fn keyboard_text_validation_rejects_character_limit_and_nul() {
+        assert_eq!(
+            validate_keyboard_text("Dragonborn".to_owned(), 9),
+            Err(vr::EVROverlayError::RequestFailed)
+        );
+        assert_eq!(
+            validate_keyboard_text("Dragon\0born".to_owned(), 16),
+            Err(vr::EVROverlayError::RequestFailed)
+        );
+    }
+
+    #[test]
+    fn keyboard_done_overlay_event_survives_an_undersized_poll() {
+        let mut events = VecDeque::from([OverlayEvent::keyboard_done(0x1234, 0x5678)]);
+        let required_size = std::mem::offset_of!(vr::VREvent_t, data)
+            + std::mem::size_of::<vr::VREvent_Keyboard_t>();
+        let mut event = vr::VREvent_t::default();
+
+        assert!(!poll_overlay_event(
+            &mut events,
+            &mut event,
+            (required_size - 1) as u32,
+        ));
+        assert_eq!(events.len(), 1);
+        assert!(poll_overlay_event(
+            &mut events,
+            &mut event,
+            std::mem::size_of::<vr::VREvent_t>() as u32,
+        ));
+
+        let event_type = unsafe { (&raw const event.eventType).read_unaligned() };
+        let device_index = unsafe { (&raw const event.trackedDeviceIndex).read_unaligned() };
+        let keyboard = unsafe { (&raw const event.data.keyboard).read_unaligned() };
+        assert_eq!(event_type, vr::EVREventType::KeyboardDone as u32);
+        assert_eq!(device_index, vr::k_unTrackedDeviceIndexInvalid);
+        assert_eq!(keyboard.cNewInput, [0; 8]);
+        assert_eq!(keyboard.uUserValue, 0x1234);
+        assert_eq!(keyboard.overlayHandle, 0x5678);
+        assert!(events.is_empty());
+    }
+}
