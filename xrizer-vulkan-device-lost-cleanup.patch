diff --git a/src/graphics_backends/vulkan.rs b/src/graphics_backends/vulkan.rs
index 96bb1f6..21ca106 100644
--- a/src/graphics_backends/vulkan.rs
+++ b/src/graphics_backends/vulkan.rs
@@ -28,7 +28,9 @@ impl Drop for VulkanData {
 impl Drop for VulkanData {
     fn drop(&mut self) {
         unsafe {
-            self.device.device_wait_idle().unwrap();
+            if let Err(error) = self.device.device_wait_idle() {
+                warn!("Vulkan device wait failed during teardown: {error:?}");
+            }
         }
         match &self.real_data {
             // Temporary session - we created these handles, so let's destroy them
